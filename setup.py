#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name="hfwrapper",
    version="0.1",
    description="Honggfuzz wrapper",
    packages=find_packages(),
    setup_requires=[],
    install_requires=[]
)
